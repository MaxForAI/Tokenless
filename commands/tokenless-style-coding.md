---
description: Set Tokenless style to coding: dense structured output
allowed-tools: Bash
---

Run:

```bash
tokenless style coding --data-dir ~/.tokenless
```

Then report the printed `effective_style`, `style_source`, and whether the user must restart Claude Code.
